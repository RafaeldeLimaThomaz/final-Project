import { StyleSheet } from "react-native";

export default StyleSheet.create({
  menuItemsCard: {
    flexDirection: "row",
    justifyContent: "center",
    alignItems: "center",
    borderRadius: 10,
  },
});
