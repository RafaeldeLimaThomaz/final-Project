import { StyleSheet } from "react-native";

export default StyleSheet.create({
    titleInput: {
        height: 40,
        margin:15,
        marginBottom:10,
        backgroundColor: "#FF4984",
        borderWidth: 1,
        borderRadius: 10,
        borderColor: "white",
        borderStyle: "dashed",
        color: "white",
      }
});
